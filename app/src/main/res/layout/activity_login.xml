<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/backup_background"
    tools:context="com.afeka.keepitup.keepitup.LoginActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/backupButton"
            android:layout_width="150dp"
            android:layout_height="70dp"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:background="@drawable/button"
            android:fontFamily="@font/revolution"
            android:onClick="backupToFirebase"
            android:enabled="false"
            android:text="@string/backup"
            android:textColor="@color/blue"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginTop="10dp"
             />

        <Button
            android:id="@+id/restoreButton"
            android:layout_width="150dp"
            android:layout_height="70dp"
            android:layout_above="@+id/backupButton"
            android:layout_alignStart="@+id/backupButton"
            android:background="@drawable/button"
            android:fontFamily="@font/revolution"
            android:onClick="restoreFromFirebase"
            android:enabled="false"
            android:text="@string/restore"
            android:textColor="@color/blue"
            android:textSize="22sp"
            android:textStyle="bold"
             />

        <Button
            android:id="@+id/login"
            android:layout_width="150dp"
            android:layout_height="70dp"
            android:layout_marginTop="10dp"
            android:layout_alignStart="@+id/backupButton"
            android:layout_below="@+id/backupButton"
            android:background="@drawable/button"
            android:fontFamily="@font/revolution"
            android:onClick="checkLogin"
            android:text="@string/sign_in"
            android:textColor="@color/blue"
            android:textSize="22sp"
            android:textStyle="bold"
             />

    </RelativeLayout>

</android.support.constraint.ConstraintLayout>
